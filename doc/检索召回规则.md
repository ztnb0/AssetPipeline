内部图片：按原有综合向量得分返回前 20。

内部视频：按原有综合向量得分返回前 20。

外部图片：每个平台最多召回 20 条，不计算 embedding 相似度，也不进行跨平台语义重排。按包图网、Pexels、Pixabay、Unsplash、Openverse、Mixkit 的固定优先级依次返回，并保留各平台原始顺序。

外部视频：Pexels、Pixabay、Mixkit 各召回 20，按平台原始顺序各保留前 10，不计算 embedding 相似度。

前端按“图片 / 视频”两大类展示，每类再分“内部资源 / 外部资源”。

四组结果独立排序，不再把内部和外部得分混在一起比较。

外部图片和外部视频卡片的分数均表示“平台排名”，不代表语义相关度。
