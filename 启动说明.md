# 启动说明

本仓库包含两个可独立运行的项目：

| 目录 | 项目 | 说明 | 默认地址 |
| --- | --- | --- | --- |
| 根目录 | Asset Pipeline | 素材资产中心（Docker Compose） | http://localhost:3000 |
| `chat2chart/` | chat2chart | 一句话生成数据图表 | http://localhost:8787 |

---

## 一、下载仓库

```powershell
git clone git@github.com:ztnb0/AssetPipeline.git
cd AssetPipeline
```

> 使用 HTTPS 时：`git clone https://github.com/ztnb0/AssetPipeline.git`

---

## 二、启动主项目 Asset Pipeline

### 环境要求

- Windows / Linux / macOS
- Docker Desktop（或 Docker Engine + Compose Plugin）
- 至少 8 GB 内存，建议 16 GB
- 能联网访问 Qwen 视觉与向量服务（默认指向 `114.113.151.16`）

### 启动步骤

```powershell
# 1. 创建环境配置
Copy-Item .env.example .env

# 2. 按需修改 .env（至少确认 Qwen 服务地址与模型名）
#    VISION_API_BASE_URL / VISION_MODEL / EMBEDDING_API_BASE_URL

# 3. 构建并启动全部服务
docker compose up -d --build

# 4. 查看状态
docker compose ps
```

### 访问地址

- 素材中心：http://localhost:3000
- API 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/health
- MinIO 控制台：http://localhost:9101
- Qdrant 面板：http://localhost:6333/dashboard

### 日常运维

```powershell
# 后台启动
docker compose up -d

# 代码或依赖变化后重建
docker compose up -d --build

# 查看状态
docker compose ps

# 查看后端日志
docker compose logs -f backend

# 停止并删除容器（保留数据卷）
docker compose down
```

> 电脑重启后先启动 Docker Desktop，再在项目目录执行 `docker compose up -d`。
> 不要在需要保留素材时执行 `docker compose down -v`，它会删除 MySQL/MinIO/Whisper 数据卷。

### 常见问题

| 现象 | 处理 |
| --- | --- |
| 打不开 localhost:3000 | 确认 Docker Desktop 已启动，执行 `docker compose ps` 检查 |
| 上传后一直「分析中」 | `docker compose logs -f backend` 查看；首次音频分析需下载 Whisper 模型 |
| Qwen 分析失败 | 确认 `VISION_API_BASE_URL` 以 `/v1` 结尾、模型名与 `/v1/models` 一致、能访问模型服务 |
| 换主机部署 | 修改 `.env` 的 `NEXT_PUBLIC_API_BASE_URL` 为后端可访问地址并重建前端；同时改 `CORS_ORIGINS` |

---

## 三、启动 chat2chart 子项目

### 环境要求

- Python **3.11**（与 akshare 兼容性最好）
- **opencode CLI**（headless 调用 Agent）
- **lieflat-charts** 技能
- **akshare**（取数，需联网）
- 视频模式额外需要：Node.js + Remotion 工程 + ffmpeg

### 安装前置依赖

```powershell
# 1. 安装 opencode CLI（任选其一）
npm install -g opencode-ai
# 或
irm https://opencode.ai/install | iex

# 2. 登录模型
opencode auth login

# 3. 安装 lieflat-charts 技能
git clone --depth 1 https://github.com/larashero3-dotcom/lieflat-charts "$HOME\.claude\skills\lieflat-charts"

# 4.（仅视频模式）安装 Remotion 工程
cd chat2chart/remotion
npm install
cd ..
```

### 启动步骤

```powershell
cd chat2chart
Copy-Item .env.example .env   # 按需修改（如 OPENCODE_BIN、C2C_* 等）
./start.ps1                    # 默认 http://localhost:8787
```

打开 http://localhost:8787，输入一句话 → 选择「HTML 图表 / Remotion 视频动画」→ 生成 → 预览 / 下载。

> Windows 上不要给 uvicorn 加 `--reload`（`start.ps1` 已默认不带），否则事件循环会切换成
> `SelectorEventLoop`，导致 opencode / Remotion 渲染无法启动。

### 无 token 冒烟测试

想先验证后端链路（不真正调用模型）时：

```powershell
$env:C2C_FAKE = "1"
./start.ps1
```

---

## 四、安全提醒

- `.env`、日志、`data/`、`.venv` 已被 `.gitignore` 排除，不会随 Git 上传。
- 不要把包含真实密钥的 `.env` 提交到 Git。
- 当前 Demo 无鉴权，不要将 `8000`、`3307`、`9100`、`9101`、`8787` 直接暴露到公网。
