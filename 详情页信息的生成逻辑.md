目前项目中，图片/视频的“详情页信息”不是打开详情页时生成的，而是素材上传后在后台异步生成并写入 MySQL。详情页只负责读取和展示这些数据。
上传/外部导入
    ↓
原文件保存到 MinIO
    ↓
MySQL 创建 processing 记录
    ↓
FastAPI 后台任务 process_asset()
    ├─ 图片：读取尺寸 → 生成缩略图 → Qwen 单图分析
    └─ 视频：FFprobe → 5 个时间点抽帧 → Qwen 多帧分析
    ↓
描述、场景、分类、标签、技术参数写入 MySQL
    ↓
状态变成 ready
    ↓
前端轮询并展示详情
1. 上传时创建基础记录
上传接口位于 [main.py (line 150)](E:/AssetPipeline/backend/app/main.py:150)。
收到文件后会：
根据 MIME 类型或扩展名判断是 image、video 还是 audio
检查文件是否为空、是否超过大小限制
计算 SHA-256
将原文件写入 MinIO：
media/{media_type}/未分类/{asset_id}.{suffix}
在 MySQL 创建 Asset
初始状态设置为 processing
使用 FastAPI BackgroundTasks 启动 process_asset(asset.id)
立即返回 HTTP 202
因此 202 只表示“已经进入处理流程”，并不表示详情信息生成完成。
基础数据包括：
原始文件名
MIME 类型
文件大小
素材类型
来源平台、作者、许可
MinIO 对象地址
内容哈希
创建时间
数据表定义在 [models.py (line 17)](E:/AssetPipeline/backend/app/models.py:17)。
2. 图片详情信息如何生成
核心逻辑在 [processor.py (line 69)](E:/AssetPipeline/backend/app/processor.py:69)。
图片处理步骤：
使用 Pillow 打开图片。
根据 EXIF 自动纠正图片方向。
获取原图宽高，写入 width、height。
将图片缩放到最大 640 × 640。
生成 WebP 缩略图：
thumbnails/{asset_id}.webp
将原图 Base64 编码后发送给 Qwen 多模态接口。
保存模型生成的：description
scene
tags
categories

将第一项分类作为主分类 category。
将分析版本写入：
media_metadata.analysis_version = visual-v2
图片的 AI 提示词位于 [analyzer.py (line 11)](E:/AssetPipeline/backend/app/analyzer.py:11)，要求模型返回严格 JSON，例如：
{
  "description": "画面中两名商务人士坐在会议桌旁交谈，桌上放有电脑和文件。",
  "tags": ["商务人士", "会议", "交谈", "办公室"],
  "scene": "办公室内商务人员讨论工作方案",
  "categories": ["商业"]
}
模型被要求只描述可观察事实，不能随意推断人物身份、品牌、认证资格或金融结论。
3. 视频详情信息如何生成
核心逻辑在 [processor.py (line 86)](E:/AssetPipeline/backend/app/processor.py:86)。
视频处理比图片多两个阶段。
第一阶段是 FFprobe 技术解析：
时长 duration
宽度、高度
封装格式
总码率
视频/音频流
编码格式
帧率
采样率
声道数
这些信息保存到 media_metadata。
第二阶段是视频抽帧和 AI 分析：
在视频时长的 5%
25%
50%
75%
95%
分别提取一帧，最多五帧。实现位于 [processor.py (line 52)](E:/AssetPipeline/backend/app/processor.py:52)。
FFmpeg 抽帧时把画面宽度限制在 1280 像素。第一张抽出的帧同时作为视频缩略图：
thumbnails/{asset_id}.jpg
随后五帧按照时间顺序一次性发送给 Qwen，并附带：
每一帧的时间点
视频文件名
视频总时长
视频提示词要求模型先描述每一帧，再综合整段视频，生成：
{
  "frame_analysis": [
    {
      "timestamp": 5.2,
      "description": "主持人站在演播室屏幕前"
    }
  ],
  "description": "视频主要展示主持人在财经演播室讲解市场走势。",
  "tags": ["主持人", "财经演播室", "市场走势"],
  "scene": "财经演播室主持人解读市场走势",
  "categories": ["财经", "金融市场"]
}
其中 frame_analysis 被保存在 media_metadata，但当前详情页没有直接展示逐帧描述。
4. AI 返回结果的校验
模型调用及清洗逻辑在 [analyzer.py (line 44)](E:/AssetPipeline/backend/app/analyzer.py:44)。
后端会：
去掉 Markdown 代码块
尝试解析 JSON
如果返回内容夹杂额外文字，提取第一个 JSON 对象
标签去空、去重，最多保存 20 个
分类只能来自预定义分类表
最多保留 3 个 AI 分类
没有合法分类时回退为“其他”
视频逐帧结果会重新绑定真实抽帧时间，避免模型自己生成错误时间点
Qwen 调用参数为低随机性：
temperature = 0.1
enable_thinking = false
这使详情信息相对稳定，但本质上仍然是 AI 生成结果。
5. 分析完成后的入库与文件整理
统一入口在 [processor.py (line 156)](E:/AssetPipeline/backend/app/processor.py:156)。
分析成功后：
状态变为 ready
清除 error_message
第一项分类成为主分类
原始文件从“未分类”目录移动到：
media/{image|video}/{主分类}/{asset_id}.{suffix}
分析失败则：
状态变为 failed
异常信息写入 error_message
不会生成完整详情信息
后台通过信号量限制最多同时执行两个 FFmpeg/Qwen/Whisper 分析任务。它是进程内任务，不是持久化队列，后端重启时正在运行的分析不会自动恢复。
6. 详情页如何获取和展示
后端输出结构定义在 [schemas.py (line 8)](E:/AssetPipeline/backend/app/schemas.py:8)，serialize() 会额外生成：
content_url   = /api/assets/{id}/content
thumbnail_url = /api/assets/{id}/thumbnail
对应接口位于 [main.py (line 313)](E:/AssetPipeline/backend/app/main.py:313)。
前端加载素材列表时调用：
GET /api/assets
如果存在 processing 状态的素材，每两秒重新拉取列表。代码在 [page.tsx (line 111)](E:/AssetPipeline/frontend/app/page.tsx:111)。
点击素材卡片后，当前实现不会再次请求详情接口，而是直接把列表中的完整 Asset 对象交给 AssetDetail 弹窗。详情组件位于 [page.tsx (line 465)](E:/AssetPipeline/frontend/app/page.tsx:465)。
详情页展示三类信息：
内容信息：名称、分类、具体场景、描述、标签
媒体预览：图片显示原图，视频显示播放器和缩略图
技术信息：类型、格式、大小、时长、分辨率、编码、码率、帧率、来源、作者、许可、上传时间
内容信息和标签可以修改，保存时调用：
PATCH /api/assets/{id}
修改第一项分类时，后端还会同步移动 MinIO 中原文件的存储目录。