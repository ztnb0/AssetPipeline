**本地素材**

```
相关度 = 75% × 向量相似度 + 关键词命中加分
       = 0.75 × vector_score + (命中关键词 ? 0.25 : 0)
```

其中：

- 向量相似度：搜索词通过 Qwen3-Embedding-8B 转成 4096 维向量，与素材的名称、描述、场景、分类、标签、视频画面描述和转写文本计算 Cosine 相似度。
- Qdrant 只召回向量相似度不低于 `0.25` 的素材。
- 关键词匹配会先扩展同义词，再对名称、描述、场景、分类和标签做 `LIKE` 匹配。
- 关键词分目前是二值加分：只要命中任何扩展词就固定加 `0.25`，命中次数和字段数量不会继续提高分数。

例如：

```
向量相似度 0.80，命中关键词：
0.75 × 0.80 + 0.25 = 0.85，即页面显示 85%

向量相似度 0.90，未命中关键词：
0.75 × 0.90 = 0.675，即页面显示 68%

仅关键词命中：
相关度固定为 0.25，即 25%
```

实现位于 [main.py (line 344)](E:/AssetPipeline/backend/app/main.py:344)，向量召回阈值位于 [vector_store.py (line 140)](E:/AssetPipeline/backend/app/vector_store.py:140)。

**外部素材**

Pexels、Pixabay、Unsplash、Openverse 的结果使用：

```
相关度 = 85% × 语义相似度 + 15% × 来源站内排名分
```

站内排名分为：

```
rank_score = 1 - (rank - 1) / 9
```

因此第 1 名为 `1.0`，第 10 名为 `0`。外部素材的语义相似度只基于其 `title + author + media_type` 计算。如果 Embedding 服务不可用，则完全使用站内排名分。代码位于 [main.py (line 405)](E:/AssetPipeline/backend/app/main.py:405)。

最终取本地前 10 条、外部前 20 条，按各自算出的 `score` 混合降序排列。前端只是将该分数乘以 100 并四舍五入显示，见 [page.tsx (line 547)](E:/AssetPipeline/frontend/app/page.tsx:547)