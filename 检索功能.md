+ ```
  用户输入 q
     ├─ 关键词召回
     │    ├─ 本地同义词扩展
     │    ├─ Qwen 查询扩展
     │    └─ MySQL LIKE 匹配
     │
     └─ 语义召回
          ├─ Qwen3-Embedding-8B 生成查询向量
          └─ Qdrant Cosine 相似度搜索
  
  两路结果取并集
     ↓
  计算综合分数
     ↓
  按综合分数倒序返回
  ```

  ## 关键词检索部分

  关键词检索会在 MySQL 的这些字段中匹配：

  - `original_name`
  - `description`
  - `scene`
  - `category`
  - `categories`
  - `tags`

  在查询前，系统还会做两类扩展：

  1. `semantic.py` 中的本地同义词扩展，例如：

  ```
  对谈 → 访谈、谈话、对话、采访、交流
  AI → 人工智能、智能模型、机器学习
  财经 → 经济、金融、商业、市场
  ```

  1. 调用 Qwen 生成最多 8 个相关表达。

  最终使用 SQL 的 `LIKE "%关键词%"` 召回素材。

  ## 语义检索部分

  查询文本会发送给：

  ```
  Qwen3-Embedding-8B
  ```

  生成 4096 维查询向量，再到 Qdrant 中进行 Cosine 相似度搜索。

  Qdrant 返回类似：

  ```
  [
    {
      "asset_id": "asset-001",
      "score": 0.82
    },
    {
      "asset_id": "asset-002",
      "score": 0.71
    }
  ]
  ```

  同时，`media_type` 和 `category` 会作为 Qdrant 过滤条件，例如：

  ```
  media_type = video
  category = 财经
  status = ready
  ```

  ## 两者的权重

  当前代码中的综合分数是：

  ```
  combined_score = (
      0.75 * vector_score
      + (0.25 if asset.id in keyword_ids else 0.0)
  )
  ```

  所以设计权重是：

  ```
  语义检索：75%
  关键词命中：25%
  ```

  也就是说：

  - 只被向量召回的素材：

  ```
  综合分 = 0.75 × 向量相似度
  ```

  - 同时被关键词和向量召回的素材：

  ```
  综合分 = 0.75 × 向量相似度 + 0.25
  ```

  - 只被关键词召回、没有向量分数的素材：

  ```
  综合分 = 0.25
  ```

  代码位置在 [main.py (line 323)](E:/AssetPipeline/backend/app/main.py:323)。

  ## 一个具体例子

  假设搜索：

  ```
  理财师和客户讨论投资方案
  ```

  素材 A：

  ```
  向量相似度 = 0.80
  关键词命中 = 是
  ```

  综合分数：

  ```
  0.75 × 0.80 + 0.25 = 0.85
  ```

  素材 B：

  ```
  向量相似度 = 0.90
  关键词命中 = 否
  ```

  综合分数：

  ```
  0.75 × 0.90 = 0.675
  ```

  虽然素材 B 的纯语义相似度更高，但素材 A 同时命中了关键词，因此最终素材 A 排在前面。

  ## 一个需要注意的技术细节

  当前“25% 关键词权重”不是一个经过归一化的关键词相关度，而是一个二值加分：

  ```
  命中关键词：+0.25
  没有命中：+0
  ```

  所以严格说，当前实现是：

  ```
  向量相似度主排序
  + 关键词命中固定加分
  ```

  而不是两个标准化分数的严格加权平均。

  此外，当前搜索结果是两路召回结果的并集：

  ```
  asset_ids = keyword_ids | set(vector_scores)
  ```

  因此：

  - 关键词搜不到，但语义相近的素材仍可能被召回
  - 语义搜不到，但关键词精确命中的素材也会保留
  - Qdrant 服务不可用时，会自动退化为关键词搜索
  - 不带 `q` 时不会进行语义搜索，只按创建时间倒序返回

  所以目前的实际策略可以概括为：

  ```
  向量相似度负责发现语义相关素材
  关键词命中负责提高精确匹配素材的排名
  最终权重：向量 75%，关键词命中加分 25%
  ```